<nav class="navbar navbar-expand-lg">
  <a class="skip-navigation" href="#main-content">
    Skip to main content
  </a>
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "OSU_logo_horizontal.png", class: "d-inline-block align-top" %>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-collapse" aria-controls="navbarSupported">
    <i class="fa fa-bars fa-3x" label="toggle navigation"></i>
  </button>
  <div class="collapse navbar-collapse" id="nav-collapse">
    <ul class="nav navbar-nav">
      <li class="nav-item">
        <%= link_to "Admin Dashboard", rails_admin_path, :class => "nav-link"  if current_user && current_user.admin? %>
      </li>
      <li class="nav-item">
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => "nav-link" if current_user %>
        <%= link_to "Login", user_session_path, :class => "nav-link" if current_user.nil? %>
      </li>
    </ul>
    <!--
    <%= form_tag(root_path, :method => :get) do %>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag 'Search', :method => :get %>
    <% end %>
    -->
  </div>
  <div class="sub-nav">
    <h1>LibNav::Rails</h1>
  </div>
  <div class="search-nav">
    <%= form_tag(root_path, :method => :get, class: "form-group row overflowing") do %>
      <%= text_field_tag :search, params[:search], class: "form-control col-8" %>
      <%= submit_tag "Search", :method => :get, :class => "col-4" %>
    <% end %>
  </div>
</nav>
